id: metrics-validation

engine:
  repository:
    enabled: false

processors:
  - id: source
    type: generate_records
    config:
      records:
        - id: 1
        - id: 2

  - id: destination
    type: log_records

connections:
  - from: source
    relationship: success
    to: destination
